#include <stdio.h>
int main()
{
    int N;
    scanf("%d", &N);

        if ( N % 3==0)
        {
            printf("YES", N);
        }
        else
        {
            printf("NO", N);
        }
    
    return 0;
}